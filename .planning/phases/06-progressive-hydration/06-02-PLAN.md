---
phase: 06-progressive-hydration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [index.html]
autonomous: false
requirements: [HYDRA-01, HYDRA-02, HYDRA-03]

must_haves:
  truths:
    - "User sees smooth fade-in of source values, not instant snap"
    - "User scrolled halfway down board keeps scroll position when source loads"
    - "User with prefers-reduced-motion sees values appear instantly"
    - "Full re-renders for picks and filter changes still work correctly"
  artifacts:
    - path: "index.html"
      provides: "Complete progressive hydration system"
      contains: "hydrateSourceColumn"
---

<objective>
Verify the progressive hydration implementation end-to-end across both themes, viewports, and reduced-motion settings.

Purpose: Confirm all three HYDRA requirements are met through visual verification.
Output: Verified hydration system with user approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-progressive-hydration/06-CONTEXT.md
@.planning/phases/06-progressive-hydration/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify progressive hydration across all scenarios</name>
  <files>index.html</files>
  <action>
Visual verification of all hydration behaviors.
  </action>
  <verify>User confirms hydration works correctly</verify>
  <done>User approves progressive hydration implementation</done>
  <what-built>Progressive hydration system: targeted DOM updates for source columns with valIn fade-in animation, scroll preservation during source loading, staggered animation delays, and prefers-reduced-motion support.</what-built>
  <how-to-verify>
    **Hydration animation (HYDRA-01):**
    1. Clear localStorage (`localStorage.clear()` in console) and reload the app
    2. Connect to a draft — FC loads first (instant, as primary source)
    3. Watch the KTC column — values should fade in smoothly with a subtle upward slide, NOT snap in all at once
    4. Watch DD and DP columns as they load — same smooth fade-in behavior
    5. Notice the top-to-bottom cascade: top rows animate slightly before lower rows

    **Scroll preservation (HYDRA-02):**
    6. After all sources are loaded, clear just one source: `S.sources.ktc.status='idle';S.sources.ktc.data={};` in console
    7. Scroll halfway down the board
    8. Run `loadKTC()` in console
    9. Verify: your scroll position does NOT jump — KTC values fade in at your current scroll position
    10. The board should remain perfectly still during hydration

    **Reduced motion (HYDRA-03):**
    11. In DevTools, enable "Emulate prefers-reduced-motion: reduce" (Rendering panel)
    12. Repeat step 6-8: clear a source and reload it
    13. Verify: values appear INSTANTLY with no visible animation or delay
    14. Disable the reduced motion emulation

    **Full re-render still works:**
    15. Click position filter buttons (QB, RB, WR, TE, ALL) — board should re-render correctly
    16. If in a live draft, verify picks still update the board correctly
    17. Toggle dark/light mode — hydrated values should maintain their display

    **Both themes:**
    18. Verify fade-in looks good in both light and dark mode
    19. Check that source-specific colors (KTC purple, DD pink, DP green) are correct after hydration
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Source columns fade in with valIn animation (HYDRA-01)
- Board scroll preserved during source loading (HYDRA-02)
- Reduced motion users see instant values (HYDRA-03)
- Full re-renders work for picks and filters
</verification>

<success_criteria>
All three HYDRA requirements verified working by user across both themes and reduced-motion settings.
</success_criteria>

<output>
After completion, create `.planning/phases/06-progressive-hydration/06-02-SUMMARY.md`
</output>
