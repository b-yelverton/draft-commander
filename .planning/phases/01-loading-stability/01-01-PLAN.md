---
phase: 01-loading-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [LOAD-01, LOAD-02, LOAD-03]

must_haves:
  truths:
    - "Board header always shows RK, POS, PLAYER, TM, FC, KTC, DD, DP, and delta-RNK columns regardless of which sources are loaded"
    - "Board player rows always include all source value columns with '--' placeholder when source is pending"
    - "Sidebar shows shimmer skeleton cards in Recommendations section while sources are loading"
    - "Sidebar shows shimmer skeleton rows in Scarcity section while sources are loading"
    - "Skeleton shimmer animation uses warm earth-tone gradient consistent with app design system"
  artifacts:
    - path: "index.html"
      provides: "Shimmer CSS, always-render board columns, sidebar skeleton placeholders"
      contains: "@keyframes shimmer"
  key_links:
    - from: "renderBoard() header"
      to: "renderBoard() rows"
      via: "identical column widths (36px per source, 50px delta)"
      pattern: "width:36px"
    - from: ".skeleton CSS class"
      to: "renderRoom() sidebar"
      via: "skeleton-card and skeleton-row classes in recommendations and scarcity"
      pattern: "skeleton"
---

<objective>
Stabilize board column layout and add skeleton loading placeholders.

Purpose: Eliminate the layout shift that occurs when background sources (KTC, DD, DP) finish loading and columns appear/disappear. Add shimmer skeleton placeholders to the sidebar so users see content-shaped loading states instead of a text message.

Output: Board always renders all source columns (with "--" for pending sources), sidebar shows shimmer skeletons while sources load.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-loading-stability/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shimmer skeleton CSS and val-pending styles</name>
  <files>index.html</files>
  <action>
Add the following CSS rules inside the existing `<style>` block (before the closing `</style>` tag, near the other animation keyframes):

1. Add `@keyframes shimmer` animation:
   - `0% { background-position: -200px 0; }` to `100% { background-position: 200px 0; }`

2. Add `.skeleton` base class:
   - `background: linear-gradient(90deg, var(--surface2) 25%, #e8e0d5 50%, var(--surface2) 75%)`
   - `background-size: 400px 100%`
   - `animation: shimmer 1.5s ease-in-out infinite`
   - `border-radius: var(--radius-md)`

3. Add `.skeleton-card` class (for recommendation card placeholders):
   - `height: 80px`
   - `margin-bottom: var(--sp-sm)`
   - `border-radius: var(--radius-lg)`

4. Add `.skeleton-row` class (for scarcity row placeholders):
   - `height: 28px`
   - `margin-bottom: var(--sp-xs)`
   - `border-radius: var(--radius-sm)`

5. Add `.val-pending` class (for "--" placeholders in board columns):
   - `color: var(--text3)`
   - `opacity: 0.4`

6. Add `prefers-reduced-motion` override for `.skeleton` to disable shimmer animation (set `animation: none`), matching the existing pattern at lines 341-343.
  </action>
  <verify>
    <automated>grep -c "@keyframes shimmer" index.html</automated>
  </verify>
  <done>Shimmer keyframes, skeleton-card, skeleton-row, and val-pending CSS classes exist in the style block. Reduced-motion users see static placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Always render all board columns and add sidebar skeleton placeholders</name>
  <files>index.html</files>
  <action>
**Board header (line ~1106-1113 in renderBoard):**
Remove the conditional `${hasKtc?...''}` / `${hasDd?...''}` / `${hasDp?...''}` wrapping on header column spans. Always render all 8 columns:
- RK (30px), POS (26px), PLAYER (flex:1), TM (28px), FC (36px, color fc-color), KTC (36px, color ktc-color), DD (36px, color dd-color), DP (36px, color dp-color), delta-RNK (50px, center)

The header should be unconditional -- all source column headers always visible.

**Board rows (line ~1126-1136 in renderBoard):**
Remove the conditional `${hasKtc?...:''}` wrapping on row value spans. Always render all source columns:
- FC: `#${p.fcRank}` (always live since FC is loaded synchronously)
- KTC: `${hasKtc ? (p.ktcRank ? '#'+p.ktcRank : '--') : '<span class="val-pending">--</span>'}`
- DD: `${hasDd ? (p.ddRank ? '#'+p.ddRank : '--') : '<span class="val-pending">--</span>'}`
- DP: `${hasDp ? (p.dpRank ? '#'+Math.round(p.dpRank) : '--') : '<span class="val-pending">--</span>'}`
- Delta RNK: Always render the span (width:50px). When hasKtc is true AND p.ktcRank exists, show the delta with color. When hasKtc is true but no ktcRank, show "--". When hasKtc is false, show `<span class="val-pending">--</span>`.

Keep the `hasKtc`, `hasDd`, `hasDp` boolean variables at the top of renderBoard -- they are still used for VALUE logic (choosing between real data vs placeholder display). Only the CONDITIONAL RENDERING of the column spans is removed.

**Sidebar Recommendations skeleton (line ~1071-1075 in renderRoom):**
Replace the current text-based "Loading value sources..." message with shimmer skeleton cards. The logic at line 1075 currently falls through to a text div when `recs.length` is 0. Change this fallback to:
- Check if any non-FC source is still loading or idle: `['ktc','dd','dp'].some(k => S.sources[k].status === 'loading' || S.sources[k].status === 'idle')`
- If sources still loading: render 3 skeleton-card divs with decreasing opacity (1.0, 0.7, 0.4) and the last one at height:60px
- If all sources are done but still no recs: keep the existing text message about BPA recommendations

**Sidebar Scarcity skeleton (line ~1082-1090 in renderRoom):**
Wrap the scarcity section content in a check: if FC source is not live yet, show 4 `.skeleton-row` placeholders (one per position). Otherwise render the existing scarcity content as-is. FC is loaded synchronously so this skeleton would only flash very briefly (or not at all), but it ensures structural stability.

Maintain the existing compressed code style -- semicolons between statements, short variable names, template literals for HTML.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const hasAlwaysKTC=h.includes('color:var(--ktc-color)\">KTC</span>') && !h.match(/hasKtc\s*\?\s*'<span[^']*KTC/);const hasSkel=h.includes('skeleton-card');console.log('always-KTC-header:',hasAlwaysKTC,'sidebar-skeleton:',hasSkel);if(!hasAlwaysKTC||!hasSkel)process.exit(1);"</automated>
  </verify>
  <done>Board header and rows always render all source columns unconditionally. Pending sources show "--" with val-pending styling. Sidebar recommendations show 3 shimmer skeleton cards while sources load. Scarcity section shows skeleton rows when FC is not yet live.</done>
</task>

</tasks>

<verification>
Open index.html in browser. Connect to a draft. On room entry:
1. Board should immediately show all column headers (FC, KTC, DD, DP, delta-RNK) even before background sources load
2. Board rows should show "--" in KTC, DD, DP columns while those sources are loading
3. Sidebar should show shimmer skeleton cards in the Recommendations section
4. As each source finishes loading, "--" placeholders replace with real rank values
5. No column width snapping or layout shift should occur
</verification>

<success_criteria>
- Board columns are structurally stable from first render to all-sources-loaded
- Sidebar shows animated shimmer skeletons instead of text "Loading value sources..."
- No conditional column rendering remains in renderBoard header or row templates
- Shimmer animation respects prefers-reduced-motion
</success_criteria>

<output>
After completion, create `.planning/phases/01-loading-stability/01-01-SUMMARY.md`
</output>
